<div>
	<h4>Comment by <%= comment.author_name %>, posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later (<%= comment.created_at %>).</h4>
	<p class="comment"><%= comment.body %></p>
</div>

